%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Level
  %tbody
    - events.each do |event|
      %tr
        %td= link_to(event.display_name, office_event_path(event))
        %td= event.level