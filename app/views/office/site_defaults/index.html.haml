.panel.panel-default
  .panel-heading
    = controls do |c|
      - c << new_button
    %h3= pluralize(@site_defaults.size, 'Key')

  .panel-body
    = render 'site_defaults', site_defaults: @site_defaults
