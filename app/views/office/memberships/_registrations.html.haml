%table.table.table-striped.tablesorter
  %thead
    %tr
      %th Title
      %th Level
      %th.center Status
      %th.center Completed
  %tbody
    - registrations.each do |r|
      %tr
        %td= link_to_show(office_registration_path(r), r.event_name)
        %td= r.level
        %td.center= r.status
        %td.center= color_dot(r.completed)
