.panel.panel-default
  .panel-heading
    = controls do |c|
      - c << new_button
    %h3 Upcoming #{pluralize(@events.size, 'Event')}

  .panel-body
    = render 'filter'
    = render 'chart'
    = render 'events', events: @events
