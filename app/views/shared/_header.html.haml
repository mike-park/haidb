%header#header
  %nav
    %ul
      - if current_user
        %li= link_to "Signout", destroy_user_session_path
      - else
        %li= link_to "Signin", new_user_session_path
  %nav
    = tabs_tag do |tab|
      = tab.dashboard "Dashboard", office_dashboard_path
      = tab.angels "Angels", summary_office_angels_path
      = tab.events "Events", office_events_path

  
